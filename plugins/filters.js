import Vue from 'vue';

// Vue.filter('shippingDate', (iso8601Date) => {
//     const parsed = parseIso8601(iso8601Date);
//     console.log('parsed date', parsed);

//     if(parsed.month && parsed.day) {
//         return this.$t(`month_${parsed.month}_short`) + ' ' + parsed.day;
//     }
// });
